get:
  tags:
    - Resources
  description: Получить список всех ресурсов
  operationId: getAllResources
  responses:
    '200':
      description: Список всех ресурсов
      content:
        application/json:
          schema:
            $ref: 'resources-model.yaml#/components/GetAllResourcesResponse'
  
